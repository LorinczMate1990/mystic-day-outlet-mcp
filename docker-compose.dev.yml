services:
  backend:
    build:
      dockerfile: Dockerfile.dev
    volumes:
      - ./backend:/app
    environment:
      - NODE_ENV=development
    command: npm run start:dev

  frontend:
    deploy:
      replicas: 0
