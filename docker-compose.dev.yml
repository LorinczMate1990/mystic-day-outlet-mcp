services:
  mcpservice:
    build:
      dockerfile: Dockerfile.dev
    volumes:
      - ./mcpservice:/app
    environment:
      - NODE_ENV=development
    command: npm run start:dev

  frontend:
    deploy:
      replicas: 0
